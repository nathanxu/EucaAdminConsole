define([
    'models/eucacollection',
    'models/snapshot'
], function(EucaCollection, Snapshot) {
    var Snapshots = EucaCollection.extend({
	model: Snapshot,
	url: 'ea.snapshot.SnapshotAction$query.json',
    });
    return Snapshots;
});
